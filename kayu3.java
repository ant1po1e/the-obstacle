import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class kayu3 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class kayu3 extends Ground
{
    /*GreenfootImage img = getImage();
        img.scale(35, 35);
        setImage(img);
     * Act - do whatever the kayu3 wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public kayu3()
    {
        GreenfootImage img = getImage();
        img.scale(400, 18);
        setImage(img);
    }
}
